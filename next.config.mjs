/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['aceternity.com', 'assets.aceternity.com','images.unsplash.com'], // Add both domains here
    },
  };
  
  export default nextConfig;
  